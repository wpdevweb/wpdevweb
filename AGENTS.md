# AGENTS.md

Profile README is a static public projection.

Rules:
- no private repository names, private task topology, secrets or credentials;
- no remote badges, dynamic images, client scripts, analytics or API calls in README;
- percentages require explicit gate denominators; inventory coverage is not product readiness;
- update `dashboard.snapshot.json`, run render, then verify before merge;
- stale public snapshots remain dated; never invent live status.
